package gui;

// Зверніть увагу, оскільки це не повний проект і тут немає всіх необхідних класів то код прописаний в коментарях

public class Main {

    public static void main(String[] args) {

        WidgetFactory factory;
        // Тепер в залежності від потреби вибираємо платформу

        /*
        if(currentOs == Windows) {
        factory = new WindowsWidgetFactory();
        }else if(currentOs == MacOs) {
        factory = new MacWidgetFactory();
        }else {
        throw new IllegalArgumentException("Invalid OS")
         */

        // створюємо фобрику

        /*
        Button button = factory.createButton();
        TextArea textArea = factory.createTextArea();
        */
    }

}
